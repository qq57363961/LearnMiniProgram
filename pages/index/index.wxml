<!--index.wxml-->
<!-- index.wxml   index.js   detail.js   detail.wxml -->
<view>组件间传值：利用本地缓存setStorageSync()、getStorageSync()</view>
<view wx:for="{{dataList}}">
  <view bindtap="getDetail" data-index="{{index}}">{{item.name}}--{{item.no}}</view>
</view>