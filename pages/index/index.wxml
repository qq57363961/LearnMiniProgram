<!--index.wxml-->
<view>组件间传值</view>
<text>方式一：通过url传值\n 例：从index页面向detail页面传值\n</text>
<view wx:for="{{proList}}" bindtap="toDetail" data-index="{{index}}">
  <view>姓名：{{item.name}}</view>
  <view>号码：{{item.no}}</view>
</view>